# Redirect direct index.html access to root
/index.html    /    301

# Serve static assets directly (CRITICAL: These must come before catch-all)
/assets/*      /assets/:splat  200
/vite.svg      /vite.svg       200
/favicon.ico   /favicon.ico    200

# API routes should not be handled by SPA (if any)
/api/*         /api/:splat     404

# Handle specific SPA routes explicitly
/signin        /index.html     200
/signup        /index.html     200
/dashboard     /index.html     200
/dashboard/*   /index.html     200
/profile       /index.html     200
/send          /index.html     200
/recurring     /index.html     200
/transactions  /index.html     200
/notifications /index.html     200
/forgot-password /index.html   200
/reset-password  /index.html   200

# Catch-all fallback for SPA (must be last)
/*             /index.html     200
