# Docker Environment Configuration for Wallet Application
# This file contains Docker-specific environment variables
# Copy this to .env.docker.local and customize for your local development

# ==============================================
# DATABASE CONFIGURATION
# ==============================================

# MongoDB Configuration for Docker Compose
# The service name 'mongodb' resolves to the MongoDB container
MONGODB_URI=mongodb://admin:walletpass123@mongodb:27017/wallet_db?authSource=admin

# Alternative local MongoDB (if running MongoDB locally instead of Docker)
# MONGODB_URI=mongodb://localhost:27017/wallet_db

# ==============================================
# BACKEND CONFIGURATION  
# ==============================================

# Server Configuration
NODE_ENV=development
PORT=3000

# JWT Configuration
# ⚠️  CRITICAL: Change JWT_SECRET in production!
# Generate a strong secret using: openssl rand -hex 64
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-environment
JWT_EXPIRY=24h

# Security & Rate Limiting
MAX_LOGIN_ATTEMPTS=5
LOGIN_TIMEOUT=900000

# CORS Configuration (Docker networking)
# Frontend container can be accessed via service name or localhost
FRONTEND_URL=http://localhost:5173

# ==============================================
# FRONTEND CONFIGURATION
# ==============================================

# API Configuration
# Points to backend service (container-to-container communication)
VITE_API_URL=http://localhost:3000

# Request timeout (30 seconds)
VITE_API_TIMEOUT=30000

# Feature flags
VITE_ENABLE_MOCK_API=false

# ==============================================
# DOCKER-SPECIFIC SETTINGS
# ==============================================

# Compose project name (optional)
COMPOSE_PROJECT_NAME=wallet

# Docker development mode
DOCKER_DEV_MODE=true

# Volume mount options
DOCKER_VOLUME_DRIVER=local

# Network configuration  
DOCKER_NETWORK_NAME=wallet-network

# ==============================================
# OPTIONAL: EXTERNAL SERVICES
# ==============================================

# Uncomment and configure if using external services

# Email service (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Redis (for session storage/caching)
# REDIS_URL=redis://redis:6379

# Logging level
LOG_LEVEL=debug

# ==============================================
# PRODUCTION OVERRIDES
# ==============================================
# For production deployment, create .env.production with:
# - Strong JWT_SECRET
# - Production MongoDB URI
# - Proper CORS origins
# - SSL certificates configuration
# - Monitoring and logging configuration
